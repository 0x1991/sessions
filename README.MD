Sessions service
================
Http REST API service for implementation users session logic, based on [Tarantool](http://tarantool.org/).


Install by docker
-----------------
`docker-compose -f docker-compose.yml -f dc.local.yml up -d`